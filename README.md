Executar os seguintes comandos para que o projeto funcione localmente:

npm install express,
npm install nodemon,
npm install mysql2,
npm install knex,
npm install cors,
npm istall --save axios,
